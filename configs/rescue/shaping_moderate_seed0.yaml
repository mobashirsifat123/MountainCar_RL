extends: ../full/dqn_replay.yaml
experiment: {name: rescue_shaping_moderate_seed0, condition: rescue_shaping_moderate, training_seed: 0}
reward_shaping: {enabled: true, beta: 0.5}
logging:
  output_dir: results/raw/rescue/shaping_moderate_seed0
  checkpoint_dir: checkpoints/rescue/shaping_moderate_seed0
