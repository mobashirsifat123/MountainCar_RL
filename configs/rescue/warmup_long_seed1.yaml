extends: warmup_long_seed0.yaml
experiment: {name: rescue_warmup_long_seed1, training_seed: 1}
logging:
  output_dir: results/raw/rescue/warmup_long_seed1
  checkpoint_dir: checkpoints/rescue/warmup_long_seed1
