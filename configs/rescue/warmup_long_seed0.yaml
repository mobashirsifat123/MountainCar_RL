extends: ../full/dqn_replay.yaml
experiment: {name: rescue_warmup_long_seed0, condition: rescue_warmup_long, training_seed: 0}
agent: {warmup_steps: 5000}
logging:
  output_dir: results/raw/rescue/warmup_long_seed0
  checkpoint_dir: checkpoints/rescue/warmup_long_seed0
