extends: ../full/dqn_replay.yaml
experiment: {name: rescue_epsilon_slow_seed0, condition: rescue_epsilon_slow, training_seed: 0}
agent: {epsilon: {decay_steps: 40000}}
logging:
  output_dir: results/raw/rescue/epsilon_slow_seed0
  checkpoint_dir: checkpoints/rescue/epsilon_slow_seed0
