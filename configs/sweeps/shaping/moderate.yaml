extends: no_shaping.yaml
experiment:
  name: shaping_ablation_moderate_seed0
  condition: dqn_replay_shaping_moderate
reward_shaping:
  enabled: true
  beta: 0.5
logging:
  output_dir: results/raw/validation/shaping_moderate_seed0
  checkpoint_dir: checkpoints/validation/shaping_moderate_seed0
