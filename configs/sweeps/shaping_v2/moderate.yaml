extends: no_shaping.yaml
experiment: {name: tuning_shaping_moderate_seed0, condition: dqn_replay_shaped_moderate}
reward_shaping: {enabled: true, beta: 0.5, eta: 0.5}
logging: {output_dir: results/raw/tuning/shaping/moderate_seed0, checkpoint_dir: checkpoints/tuning/shaping/moderate_seed0}
