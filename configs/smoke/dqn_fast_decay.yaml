extends: dqn_replay.yaml
experiment:
  name: dqn_replay_fast_decay_smoke
  condition: dqn_replay_fast_decay
agent:
  epsilon:
    decay_steps: 8
logging:
  output_dir: results/raw/smoke/dqn_replay_fast_decay_seed0
  checkpoint_dir: checkpoints/smoke/dqn_replay_fast_decay_seed0
